version: 0.2
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file cloudformation.yaml --s3-bucket codepipeline-us-west-2-124978640664 
                                   --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  files:
    - cloudformation.yaml
    - outputSamTemplate.yaml